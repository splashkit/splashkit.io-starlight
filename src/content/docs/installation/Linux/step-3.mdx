---
title: Install SplashKit
description: Install SplashKit on Linux.
sidebar:
  label: 3. SplashKit
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import { Steps } from '@astrojs/starlight/components';

[SplashKit](https://splashkit.io) is a beginner’s all-purpose software toolkit that will allow you to create fun and exciting programs more easily, especially for Graphical User Interface (GUI) programs.

Follow these steps to set up SplashKit on Linux and start building interactive, visually engaging applications quickly and easily!

## Installation Steps

<Steps>

1. **Run the SplashKit Installer**

   Open your Terminal and paste the following command to download and install SplashKit:

    ```shell
    bash <(curl -s https://raw.githubusercontent.com/splashkit/skm/master/install-scripts/skm-install.sh)
    ```

2. **Restart the Terminal and Test the Installation**

   After restarting the terminal, run the following command to confirm the installation:

    ```shell
    skm
    ```

    You should see messages similar to:

    ```shell
    SplashKit is installed successfully!
    Missing skm command. For help use 'skm help'
    ```

</Steps>

:::note[Optional Installation Commands]

The commands below are run in the initial installation script, and therefore are not required to be run unless you experience issues.

- To install the necessary dependencies and build the SplashKit library natively on linux, use:

   ```shell
   skm linux install
   ```

   :::caution[If you encounter dependency issues:]

   You can install the dependencies manually using the command, for your Linux distribution, below:

   <Tabs syncKey="linux-distro">
   <TabItem label="Ubuntu/Debian">

   ```shell
   sudo apt-get install cmake libpng-dev libcurl4-openssl-dev libsdl2-dev libsdl2-mixer-dev libsdl2-gfx-dev libsdl2-image-dev libsdl2-net-dev libsdl2-ttf-dev libmikmod-dev libncurses5-dev libbz2-dev libflac-dev libvorbis-dev libwebp-dev libfreetype6-dev build-essential clang
   ```

   </TabItem>
   <TabItem label="Arch">
   <Tabs syncKey="pacman-yay">
   <TabItem label="pacman">

   ```shell
   sudo pacman -S cmake libpng sdl2 sdl2_ttf sdl2_image sdl2_net sdl2_mixer sdl2_gfx clang freetype2 ncurses bzip2 flac libvorbis libmikmod libwebp
   ```

   </TabItem>
   <TabItem label="yay">

   ```shell
   yay -S cmake libpng sdl2 sdl2_ttf sdl2_image sdl2_net sdl2_mixer sdl2_gfx clang freetype2 ncurses bzip2 flac libvorbis libmikmod libwebp
   ```

   </TabItem>
   </Tabs>
   </TabItem>
   <TabItem label="Fedora">

   ```shell
   sudo dnf install cmake libpng SDL2 SDL2_ttf SDL2_image SDL2_net SDL2_mixer SDL2_gfx clang freetype ncurses bzip2 flac libvorbis libmikmod libwebp
   ```

   </TabItem>
   </Tabs>

- To install SplashKit into the system global directories, use:

   ```shell
   skm global install
   ```

:::

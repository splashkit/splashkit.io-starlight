#### Test Bitmap Collision with Circle Using Specified Coordinates and Bitmap Cell

Tests if a bitmap cell drawn using passed in x and y coordinates and bitmap cell will intersect with a circle. You can use this to detect collisions between bitmaps and circles.

Uses the [bitmap_circle_collision](/api/physics/#bitmap-circle-collision-5) function.

:::note
To test this example code you can download these [**Resources**](/usage-examples/physics/bitmap_circle_collision/bitmap_circle_collision-resources.zip).
:::
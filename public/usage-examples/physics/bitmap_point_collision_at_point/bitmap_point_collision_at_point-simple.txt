#### Test Bitmap Collision with a Point Given Another Point

Tests if a bitmap drawn at the bitmap location point would draw a pixel at a specified point. Use to check collisions between a point and a bitmap.

Uses the [bitmap_point_collision](/api/physics/#bitmap-point-collision-2) function.

:::note
To test this example code you can download these [**Resources**](/usage-examples/physics/bitmap_point_collision/bitmap_point_collision-resources.zip).
:::
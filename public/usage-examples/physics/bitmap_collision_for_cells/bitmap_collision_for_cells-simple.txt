#### Test Bitmap Collision with Another Bitmap Based on Cell and Specified Coordinates

Check if two bitmaps collide, based on their cells and the specified coordinates they are drawn to.

Uses the [bitmap_collision](/api/physics/#bitmap-collision-5) function.

:::note
To test this example code you can download these [**Resources**](/usage-examples/physics/bitmap_collision/bitmap_collision-resources.zip).
:::
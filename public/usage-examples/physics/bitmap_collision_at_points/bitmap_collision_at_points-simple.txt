#### Test Bitmap Collision with Another Bitmap At Specified Points

Check if two bitmaps collide when drawn to specified points.

Uses the [bitmap_collision](/api/physics/#bitmap-collision-2) function.

:::note
To test this example code you can download these [**Resources**](/usage-examples/physics/bitmap_collision/bitmap_collision-resources.zip).
:::